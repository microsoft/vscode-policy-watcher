{
  "name": "@vscode/policy-watcher",
  "version": "1.3.2",
  "description": "",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/microsoft/vscode-policy-watcher.git"
  },
  "author": "João Moreno",
  "license": "MIT",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "binding.gyp",
    "src/",
    "prebuilds/"
  ],
  "binary": {
    "napi_versions": [4]
  },
  "scripts": {
    "install": "prebuild-install || node-gyp rebuild",
    "rebuild": "node-gyp rebuild",
    "prebuild": "prebuild --runtime napi --all --verbose --strip",
    "test": "node -e \"console.log('Testing policy watcher...'); try { require('./index.js'); console.log('✅ Native module loaded successfully'); } catch(e) { console.error('❌ Failed to load native module:', e.message); process.exit(1); }\""
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^8.2.0",
    "prebuild-install": "^7.1.1"
  },
  "devDependencies": {
    "prebuild": "^13.0.1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "release": {
    "branches": [
      "main"
    ]
  }
}
